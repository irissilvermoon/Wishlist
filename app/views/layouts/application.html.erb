<!DOCTYPE html>
<html>
<head>
  <title>Wishlist</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootswatch/2.1.1/amelia/bootstrap.min.css" />
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <% flash.each do |key, value| %>
    <div class='flash' id='<%= key %>'>
      <%= value %>
    </div>
  <% end %>

  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="brand" href="/">Wishlist</a>
        <ul class="nav">
          <li ><%= link_to 'Wish List', new_wish_list_path %></li>

        </ul>
        <ul class="nav pull-right">
          <li><%= link_to "Sign up", new_user_registration_path %></li>
        </ul>
      </div>
    </div>
  </div>

  <div class="container">
    <%= yield %>
  </div>

  <footer>
    <div class="container">
      UW Rails Winter 2013
    </div>
  </footer>
</body>
</html>
